{% extends 'base.html.twig' %}

{% block body %}

    <article>
        <h2>{{ article.name}}</h2>
        <p>{{ article.description }}</p>
        <p>{{ article.price }}</p>
    </article>

{% endblock %}